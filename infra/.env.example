# =============================================================================
# Variables for prod mode (docker-compose up)
# Copy to .env and fill in values: cp .env.example .env
# Docker Compose automatically loads .env from this directory.
# =============================================================================

# --- API: required or with defaults in compose ---------------------------------
# Generate with: openssl rand -base64 64
JWT_SECRET=

# Resend API key (https://resend.com), prefix re_
# Without a real key: compose uses a placeholder (emails will not be sent).
RESEND_API_KEY=

# --- API: optional -------------------------------------------------------------
# Google Places API key (prefix AIza) for place search
GOOGLE_PLACES_API_KEY=

# Front URL (for CORS and links in emails)
FRONTEND_URL=http://localhost:3000

# S3 / Minio storage (defaults for local compose)
# When running locally with Minio, the compose already defaults these values
S3_BUCKET=nirby-uploads-dev
S3_REGION=eu-west-3
S3_ACCESS_KEY_ID=nirbyadmin
S3_SECRET_ACCESS_KEY=nirbyadmin123
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_URL=http://localhost:9000/nirby-uploads-dev

# Resend template IDs (optional; code has built-in defaults)
# EMAIL_VERIFICATION_TEMPLATE_ID_EN=
# EMAIL_VERIFICATION_TEMPLATE_ID_FR=
# EMAIL_PASSWORD_RESET_TEMPLATE_ID_EN=
# EMAIL_PASSWORD_RESET_TEMPLATE_ID_FR=
# EMAIL_ACCOUNT_DELETED_TEMPLATE_ID_EN=
# EMAIL_ACCOUNT_DELETED_TEMPLATE_ID_FR=
# EMAIL_COLLABORATOR_INVITE_TEMPLATE_ID_EN=
# EMAIL_COLLABORATOR_INVITE_TEMPLATE_ID_FR=

# --- Front build (optional) ---------------------------------------------------
# Mapbox token for the map (build time)
# NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=